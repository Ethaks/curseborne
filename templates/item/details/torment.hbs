<section class="tab standard-form {{tab.cssClass}}" data-group="primary" data-tab="{{tab.id}}">
	{{curse-formGroup systemFields.type formGroupOptions}}

	{{#if (eq system.type "lineage")}}
		{{curse-formGroup systemFields.lineage formGroupOptions}}
	{{/if}}
</section>
