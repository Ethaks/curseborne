<section class="tab standard-form {{tab.cssClass}}"
         data-group="primary"
         data-tab="{{tab.id}}">
    {{curse-formGroup
    systemFields.skills
    formGroupOptions
    options=skills.options
    }}
</section>
